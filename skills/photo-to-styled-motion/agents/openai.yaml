interface:
  display_name: "照片风格动态"
  short_description: "先看内置编号风格图，选中后只生成一个动态版本"
  default_prompt: "Use $photo-to-styled-motion to show the bundled numbered preset gallery without generating nine previews, ask me to type a number, then render only my selected style into verified motion outputs."
