# Yolo 模式详细指南

本文档详细说明 repo-study Yolo（快速）模式的研究流程，包括 subagent 调度、输出格式、笔记沉淀和翻译提示。

## 目录

1. [Subagent 配置与 Prompt 模板](#1-subagent-配置与-prompt-模板)
2. [并行研究（多课题）](#2-并行研究多课题)
3. [输出模板](#3-输出模板)
4. [Cheat Sheet 输出模板](#3a-cheat-sheet-输出模板)
5. [Beginner Guide 输出模板（小白指南）](#3b-beginner-guide-输出模板小白指南)
6. [技术展示文章输出模板](#3c-技术展示文章输出模板)
7. [笔记沉淀](#4-笔记沉淀)
8. [翻译功能提示](#5-翻译功能提示)
9. [Capability Discovery（能力发现）](#6-capability-discovery能力发现)

---

## 1. Subagent 配置与 Prompt 模板

使用 Agent 工具启动 subagent 执行研究，主会话不直接分析源码。

**subagent 配置**：
- subagent_type: `"Explore"`（擅长代码探索和分析）
- 颜色标识：蓝色（研究任务）
- prompt 中包含：源码路径、研究问题、输出格式要求

**subagent prompt 模板**：
```
你是代码架构分析专家。请深入分析以下仓库的源码。

**源码路径**: {项目目录}/{repo-name}/
**研究问题**: {用户的研究问题}
**文档资源**: {Phase 1 扫描到的文档路径列表，如无则为空}

请使用 Glob 搜索文件、Grep 搜索关键词、Read 阅读关键文件，完成以下分析：

### 第一步：产品认知（如果有文档资源）
1. 阅读文档资源（README.md、docs/ 目录下的指南文件）
2. 提取产品形态：这是什么类型的工具？（npm 包/CLI/Web 应用/浏览器插件/桌面应用）
3. 提取基本使用方式：安装命令、第一个命令示例、输出格式
4. 提取核心组件：工具由哪些部分组成？每个部分的作用？

### 第二步：代码分析
1. **搜索相关文件**：找到与研究问题相关的核心文件
2. **阅读关键代码**：深入理解核心实现逻辑
3. **分析架构设计**：理解模块关系、调用链和数据流
4. **提取设计模式**：发现可复用的设计模式和技巧

### 第三步：前因后果分析
对每个核心技术概念/设计决策，必须回答：
1. **是什么？** — 用简单语言解释这个概念
2. **为什么需要？** — 不用这个技术会怎样？有什么痛点？
3. **怎么实现的？** — 简要说明核心实现思路
4. **替代方案对比** — 为什么不用 X 而用 Y？

**输出要求**：
- 以「让完全不懂的人也能看懂」的原则撰写
- 按层次组织：产品认知 → 核心概念(含前因后果) → 代码原理
- 包含文件路径和行号的代码引用表
- 分步骤说明实现原理
- 每个技术概念必须解释"为什么需要"
- 如果项目有文档，标注"项目已有文档可参考：{路径}"
- 如果是中文提问，使用中文输出
```

---

## 2. 并行研究（多课题）

当用户一次性提出多个独立研究问题时，可以启动多个 subagent 并行研究：

```
// 同时启动多个研究 subagent（每个独立课题一个）
Agent({ description: "研究课题A", prompt: "..." })  // 蓝色标识
Agent({ description: "研究课题B", prompt: "..." })  // 蓝色标识
```

**并行触发条件**：
- 用户明确提出 2 个或以上独立研究问题
- 研究问题之间没有强依赖关系
- 每个课题分配一个独立的 subagent

---

## 3. 输出模板

**写作原则：让完全不懂的人也能看懂**

1. **假设读者是零基础**：不跳过基础概念，专业术语第一次出现时给出简单说明
2. **使用步骤化表达**：复杂流程拆分为有序步骤，每个步骤只做一件事
3. **提供完整示例**：代码片段要完整可运行，配置文件要给出完整内容

**输出模板：**

```markdown
## 研究发现: {研究问题}

> 一句话说明这个发现解决什么问题

### 一、产品认知（如果项目有文档或 README）

#### 这是什么？
// 用 2-3 句话说清楚产物形态（npm 包？CLI？插件？）和基本使用方式
// 给出安装命令和第一个使用示例

#### 核心组件
| 组件 | 形态 | 作用 |
|------|------|------|
// 列出工具的各个组成部分

#### 项目文档资源（如有）
> 本项目自带以下文档资源，可供深入学习：
| 文档路径 | 内容概述 |
|---------|---------|

### 二、核心概念（含前因后果）

// 对每个核心概念，按以下结构解释：

#### 概念：{概念名}
- **是什么**：一句话解释
- **为什么需要**：不用它会怎样？解决什么痛点？
- **实现思路**：核心数据流（3-5 步）

### 三、关键代码位置

| 文件 | 行号 | 说明 |
|------|------|------|
| path/to/file.ts | 123 | 核心逻辑 |

### 四、实现原理

// 分步骤说明，每步一个三级标题
// 每个技术选择解释"为什么这样做"

### 五、设计亮点

1. {亮点 1} — {为什么这样设计}
2. {亮点 2} — {为什么这样设计}

### 六、可复用模式

{可以借鉴的设计模式或技巧，提供完整代码示例}

### 七、参考资料

// 链接到项目已有文档（如有）
// 链接到相关技术资料
```

---

## 3a. Cheat Sheet 输出模板

**触发条件**：Phase 6 用户选择"生成 Cheat Sheet"或首次研究工具类项目时可选。

**写作原则**：快速查找、对比驱动、一行命令解决问题。

**输出模板：**

```markdown
# {工具名} — 速查卡 (Cheat Sheet)

> **一句话**：在 XXX 里一键/快速做 YYY。

---

## 快速安装（X 步，X 分钟）

```bash
# 安装命令（从官方 README 提取）
...
```

> **前提**：列出必要前提条件。

---

## 命令/功能速查

### {分类1：如 审查类/核心功能}

| 命令/操作 | 干什么 | 关键参数 | 示例 |
|------|--------|---------|------|
| ... | ... | ... | ... |

### {分类2}

| 命令/操作 | 干什么 | 关键参数 | 示例 |
|------|--------|---------|------|
| ... | ... | ... | ... |

---

## Before vs After：有了它之后工作流的变化

### 场景 1：{典型场景名}

| 阶段 | ❌ 没有这个工具 | ✅ 有了这个工具 |
|------|---------------|---------------|
| 第 1 步 | ... | ... |
| 第 2 步 | ... | ... |
| **结果** | ... | ... |

### 场景 2：{另一个典型场景}

（同上格式）

### 场景 3：{第三个典型场景}

（同上格式）

> **Before/After 写作要点**：
> - 场景要从用户真实工作流出发（不是从工具功能出发）
> - ❌ 列要体现用户真实的痛点（慢、麻烦、容易出错）
> - ✅ 列要体现具体改善（快了、简单了、可靠了）
> - 至少覆盖 2-3 个典型场景

---

## 决策树：我该用哪个功能？

```
你要做什么？
│
├─ {场景1} ──────→ {对应功能/命令}
│   │
│   └─ {细分场景} ─→ {对应细分功能}
│
├─ {场景2} ──────→ {对应功能/命令}
│
└─ {场景3} ──────→ {对应功能/命令}
```

---

## {特有概念的对比选择}（如果适用）

| 情况 | 推荐方案 | 为什么 |
|------|---------|--------|
| ... | ... | ... |

---

## 常用模式速查

### 模式 1：{模式名}
```bash
# 命令序列
```

### 模式 2：{模式名}
```bash
# 命令序列
```

---

## 配置文件位置（如果适用）

| 级别 | 路径 | 作用 |
|------|------|------|
| ... | ... | ... |
```

---

## 3b. Beginner Guide 输出模板（小白指南）

**触发条件**：Phase 6 用户选择"生成小白指南"。

**写作原则**：
- 假设读者零编程基础，不使用任何未解释的技术术语
- 用日常生活类比解释每一个概念
- Before/After 对比要像"讲故事"
- 步骤要手把手，复制粘贴就能成功
- 每个命令都解释"这条命令的意思是..."

**输出模板：**

```markdown
# {工具名} — 小白完全指南

> **写给谁看**：完全不懂编程的你。这篇指南会从头解释每一个概念，不假设你有任何技术背景。

---

## 目录

1. [这是什么东西？](#一这是什么东西)
2. [为什么要用它？](#二为什么要用它)
3. [和我现在的工作方式有什么不同？](#三和我现在的工作方式有什么不同)
4. [准备工作](#四准备工作)
5. [安装教程（手把手）](#五安装教程手把手)
6. [第一次使用](#六第一次使用)
7. [核心功能详解](#七核心功能详解)
8. [什么时候用什么功能？](#八什么时候用什么功能)
9. [常见问题](#九常见问题)

---

## 一、这是什么东西？

### 先理解几个概念

| 概念 | 生活类比 | 说明 |
|------|---------|------|
| {概念1如 Claude Code} | {类比，如"你的私人编程助手"} | {通俗解释} |
| {概念2如 Codex} | {类比，如"另一个编程助手，来自不同公司"} | {通俗解释} |
| {概念3如 插件} | {类比，如"充电转接头——让两个本来不互通的东西能够合作"} | {通俗解释} |

### 用一个生活类比来理解

（用日常生活场景做完整类比，如装修房子、看病、做菜等）

---

## 二、为什么要用它？

### 类比：{日常生活类比}

| 只用 {旧方式} | 用了 {这个工具} |
|-------------|---------------|
| {痛点1} | {改善1} |
| {痛点2} | {改善2} |
| {痛点3} | {改善3} |

### 具体能干什么？

| 功能 | 通俗解释 | 例子 |
|------|---------|------|
| ... | ... | ... |

---

## 三、和我现在的工作方式有什么不同？

### 场景对比

#### 场景 1：{典型场景}

| 步骤 | 😰 以前 | 😎 有了这个工具 |
|------|---------|---------------|
| 第 1 步 | ... | ... |
| 第 2 步 | ... | ... |
| **结果** | ... | ... |

（至少覆盖 2-3 个典型场景）

---

## 四、准备工作

### 你需要准备这些东西

| 序号 | 要准备什么 | 怎么检查有没有 | 没有怎么办 |
|------|-----------|-------------|-----------|
| 1 | ... | ... | ... |

### 你不需要准备什么

- ❌ 不需要 ...
- ❌ 不需要 ...

---

## 五、安装教程（手把手）

### 整体流程

```
第1步：... → 第2步：... → 第3步：...
```

### 第 1 步：{动作}
（每步解释"这条命令的意思是..."）

### 第 2 步：{动作}
...

### 验证安装成功
（给出验证方法）

---

## 六、第一次使用

### 最简单的体验（X 条命令）

（给出最简上手命令序列）

---

## 七、核心功能详解

### 功能 1：{功能名} (`/命令`)

**通俗解释**：{生活类比解释}

**什么时候用**：
- ...

**怎么用**：
```bash
# 示例命令
```

（每个功能重复此结构）

---

## 八、什么时候用什么功能？

### 按场景选

| 你的场景 | 用什么 | 一句话命令 |
|---------|--------|-----------|
| ... | ... | ... |

### 按操作流程选

**流程 A：{流程名}**
```
/命令1 → /命令2 → /命令3
```

---

## 九、常见问题

### Q：{新手最可能问的问题}？
（用通俗语言回答）

（至少覆盖 5-8 个常见问题，包括：花钱吗？安全吗？能卸载吗？等）
```

---

## 3c. 技术展示文章输出模板

**触发条件**：Phase 6 用户选择"生成技术展示文章"。

**前置条件**：至少完成过一轮研究（explorer/ 中有研究笔记）。

**写作原则**：
- 以**研究者的第三人称视角**写作（"研究了这个项目后..."）
- 面向掘金/知乎等技术社区，技术深度优先
- 有叙事弧线（6 段式），不是纯技术文档
- 有个人洞察和评价，不是纯客观转述

**详细指南** → `references/article-mode-guide.md`

### 文章生成 Subagent Prompt

```
你是一位面向技术社区的内容创作者。请根据以下研究素材，生成一篇面向掘金/知乎等技术社区的技术展示文章。

**项目名称**: {repo-name}
**项目地址**: {GitHub URL}
**研究素材**: {已有研究笔记内容摘要}
**项目文档**: {README/SKILL.md 关键内容}
**目标读者**: 有一定技术基础的开发者，对 {项目领域} 感兴趣

### 视角定位

以**深度研究者的第三人称视角**写作：
- 用"研究了这个项目后"、"让人印象深刻的是..."的角度
- 不假装是原作者，但可以提炼和总结作者的设计哲学
- 可以加入自己作为研究者的洞察和评价

### 叙事结构（必须包含 6 个阶段）

1. **惊艳开场** — 用具体场景/数字展示项目能力，引出项目名
2. **痛点共鸣** — 对比现有方案不足，列出理想方案需求
3. **快速上手** — 安装命令 + 使用场景 + 架构概览图
4. **设计哲学** — 提炼一句话设计公式，分层展开
5. **技术亮点** — 2-3 个有深度的实现剖析（含代码）
6. **开放讨论** — 提炼洞察 + 开放性问题 + 项目链接

### 技术深度要求（适配掘金/知乎）

- 代码块：关键实现必须有完整代码片段和中文注释
- 原理分析：不只说"怎么用"，要说清"为什么这样设计"
- 架构图：用 ASCII 或 Mermaid 画核心架构
- Before/After 对比：展示有无这个方案的差异

### 必须包含的内容

- 开头用具体场景/数字展示项目能力
- 对比现有方案的不足（附具体失败场景）
- 提炼项目的设计哲学（一句话公式 + 分层展开）
- 2-3 个有深度的技术实现剖析（含代码引用）
- 结尾提出开放性问题或讨论点

### 风格要求

- 技术自信但有理有据
- 有个人洞察和评价，不是纯客观转述
- 用具体代码/场景替代抽象描述
- 适当使用 emoji 和 ⬇️ 箭头引导阅读流
- 代码块使用 ``` 标注语言类型，注释使用中文

### 避免事项

- 不要写成纯技术文档或 API 说明
- 不要用"本文将介绍..."开头
- 不要罗列所有功能，只讲最有洞察的部分
- 不要缺少个人评价和设计思考

### 输出格式

完整的 Markdown 文章，可直接发布到掘金/知乎。字数 3000-5000 字。
```

### 文章输出文件

文件路径：`notes/{repo-name}-article.md`（非教程性质）

```markdown
---
title: "{项目名}：{一句话能力描述}"
date: "{生成日期}"
source_notes:
  - "{素材笔记 1 路径}"
  - "{素材笔记 2 路径}"
tags: ["技术展示", "{项目领域}"]
---

{文章正文（6 段式结构）}
```

### 质量检查

生成文章后，对照以下清单验证：

- [ ] 包含完整的 6 段叙事弧线（惊艳开场 → 痛点共鸣 → 快速上手 → 设计哲学 → 技术亮点 → 开放讨论）
- [ ] 开场有具体数字/场景
- [ ] 对比了至少 1 个现有方案不足
- [ ] 有复制即用的安装命令
- [ ] 提炼了一句话设计公式
- [ ] 有 2-3 个技术亮点（含代码分析）
- [ ] 有架构图或数据流图
- [ ] 有 Before/After 对比
- [ ] 语气自然，有研究者洞察
- [ ] 结尾有开放性问题
- [ ] 包含 GitHub 链接

---

## 4. 笔记沉淀

### Survey 模式：沉淀到 explorer/

将研究发现写入 `explorer/` 目录，格式同输出模板。

**编号规则**：
- `00-` 固定给环境准备章节（安装、配置、前置依赖），工具/CLI/库项目必须生成
- `01-` 起为正式内容（快速上手、导读、架构深入等）
- 新增文件时：扫描已有编号取最大值 +1

同步更新 `.study-meta.json` 的 `topics[]`：

- 若 topic 不存在：创建新 topic（`location: "explorer"`）
- 若 topic 已存在：在该 topic 下追加 `questions[]` 与 `artifacts[]`
- `progress` 采用持续态统计（问题数/笔记数/指南数/skill 模板数/可运行 skill 数）
- 不维护"完成态"，默认 `state: "active"`
- 完成后设置 `surveyState = "completed"`

### Incremental 模式：沉淀到 notes/

将增量问答结果写入 `notes/` 目录。
同步更新 `.study-meta.json` 的 `topics[]`：

- 新建 topic 条目，标记 `location: "notes"`
- 不更新 `surveyState`（保持 `"completed"`）

**Incremental 模式 subagent prompt 模板**：

```
你是代码分析专家。请针对以下问题分析相关源码。

**源码路径**: {项目目录}/{repo-name}/
**研究问题**: {用户的增量问题}
**已有调研**: {explorer/ 中已有的笔记摘要，供参考避免重复}

请只分析与问题直接相关的代码区域，不需要全面分析。

**输出要求**：
- 以「让完全不懂的人也能看懂」的原则撰写
- 包含文件路径和行号的代码引用
- 如果是中文提问，使用中文输出
```

**研究日志格式（RESEARCH-LOG.md）：**

```markdown
# 研究日志

## YYYY-MM-DD: {研究主题}

**研究主题**: {topic.name}

**研究问题**: {用户的研究问题}

**仓库**: [REPO_NAME](GITHUB_URL)

**核心发现**:
- {发现 1}
- {发现 2}

**进度（持续更新）**:
- questions: {n}
- notes: {n}
- guides: {n}
- skill templates: {n}
- runnable skills: {n}

---
```

---

## 5. 翻译功能提示

**条件触发**：如果用户使用中文提问，在沉淀笔记后提示翻译功能。

```markdown
💡 **提示**: 如果需要批量翻译当前项目文档，可以使用：

    /repo-study translate

该命令会按任务分组并行派发 subagent，把文档翻译到对应 `*.zh.md` 文件（不修改原始 `*.md`）。
```

**注意事项**：
- 仅在用户使用中文提问时提示
- 如果用户使用英文提问，跳过此步骤
- 保持提示简洁，不干扰主要研究流程

---

## 6. Capability Discovery（能力发现）

**触发条件**：YOLO 模式下，Step 5a.3b 强制执行。不可跳过。

**目标**：穷举项目所有可操作能力（命令、API、配置项、端点、插件钩子等），自动生成 Cheat Sheet。

### 6.1 判断项目类型

在启动 subagent 前，主会话根据以下信号判断项目类型：

| 信号 | 判断为 | 说明 |
|------|--------|------|
| 存在 `clis/` 或 `commands/` 或 `cmd/` 目录 | CLI 工具 | 扫描所有命令注册 |
| 存在 `routes/` 或 `@Controller` 或 `app.get` | REST API | 扫描所有端点 |
| 存在 `index.ts` + 大量 `export` | 库/SDK | 扫描公开 API |
| 存在 `SKILL.md` | Skill 项目 | 扫描所有能力 section |
| 存在 `manifest.json` + `background.ts` | 浏览器扩展 | 扫描所有浏览器 API |
| 存在 `plugin/` 或 `hook` 或 `middleware` | 插件系统 | 扫描所有扩展点 |
| 存在 `config/` 或 `.env.example` | 配置驱动 | 扫描所有配置项 |

可以同时属于多种类型（如一个 CLI 工具同时也是库）。

### 6.2 Capability Discovery Subagent Prompt

```
你是能力发现专家。请穷举以下项目的所有可操作能力，生成完整的速查卡片。

**源码路径**: {项目目录}/{repo-name}/
**项目类型**: {主会话判断的项目类型，可能有多种}
**已有研究笔记**: {explorer/ 和 notes/ 下的笔记摘要列表}

### 第一步：穷举扫描

根据项目类型，使用对应策略扫描：

**CLI 工具扫描策略**：
1. 搜索命令注册文件：`clis/`、`commands/`、`cmd/` 目录下的所有文件
2. 读取 manifest 文件（如 `cli-manifest.json`、`package.json` 的 `bin` 字段）
3. 搜索注册模式：`cli(`、`command(`、`.command(`、`yargs`、`commander`
4. 对每个命令，提取：名称、描述、参数列表、认证要求、输出格式
5. 读取官方文档（`docs/`、`README.md`）交叉验证命令列表完整性

**REST API 扫描策略**：
1. 搜索路由定义文件：`routes/`、`controllers/`、`api/` 目录
2. 搜索注册模式：`router.get`、`app.post`、`@Get`、`@Post`、`@Controller`
3. 对每个端点，提取：路径、方法、参数、响应格式、认证要求
4. 读取 OpenAPI spec（如存在）

**库/SDK 扫描策略**：
1. 读取 `index.ts`/`index.js` 获取所有导出
2. 搜索 `export`、`module.exports` 列出所有公开 API
3. 对每个 API，提取：函数签名、参数类型、返回值、使用示例
4. 读取 TypeScript 类型定义（如存在）

**浏览器扩展扫描策略**：
1. 读取 `manifest.json` 获取权限和入口文件
2. 扫描 `background.ts`、`content.ts`、`popup.ts`
3. 搜索所有 `chrome.*` API 调用
4. 提取消息通信协议（`sendMessage`、`onMessage`）

**插件系统扫描策略**：
1. 搜索插件注册/发现机制
2. 列出所有钩子（hook）名称和触发时机
3. 列出所有中间件和执行顺序
4. 提取插件 API 和生命周期

**配置驱动扫描策略**：
1. 读取 `.env.example`、`config/` 目录
2. 搜索配置项定义：`config.`、`settings.`、`ENV`
3. 对每个配置项，提取：名称、类型、默认值、说明

### 第二步：分类整理

将发现的能力按以下维度分类：
1. 按功能域（如：社交、电商、AI、金融等）
2. 按认证要求（公开/需登录/需 API Key）
3. 按使用频率（核心命令/高级功能/内部 API）

### 第三步：生成 Cheat Sheet

按维度生成 Cheat Sheet 文件。格式要求：

1. **frontmatter**：包含 `article_id`（格式：`OBA-{8位随机小写字母数字}`）、`tags`、`type: cheatsheet`、`created_at`
2. **标题**：`# {项目名} {维度名} 速查卡`
3. **一句话说明**：用 `>` 引用块
4. **通用用法**：安装、基本格式、全局选项
5. **分维度表格**：命令/操作 | 干什么 | 关键参数 | 示例
6. **常用工作流**：典型使用场景的命令组合
7. **输出格式说明**（如适用）

### 文件命名规则

- 每个维度一份：`{topic}-cheat-sheet.md`
- 如果项目只有一个维度或命令数 < 30：生成 `all-commands-cheat-sheet.md`
- 如果项目命令数 > 50：额外生成 `all-commands-cheat-sheet.md`（总览）+ 多个分类文件

### 输出要求

- 完整穷举，不要遗漏任何可操作能力
- 每个命令/API 必须有示例
- 按认证要求标注（🌐 Public / 🔐 Browser / 🔑 API）
- 包含常用工作流（搜索→详情→下载 等典型链路）
- 如果是中文提问，使用中文输出
- 穷举但不啰嗦：表格为主，减少大段文字
```

### 6.3 Cheat Sheet 质量检查

生成后，对照以下清单验证：

- [ ] **穷举性**：是否覆盖了 `docs/adapters/index.md` 或 `README.md` 中列出的所有功能？
- [ ] **准确性**：命令名、参数名是否与源码一致？（而非凭记忆）
- [ ] **分类合理性**：维度划分是否清晰、覆盖主要使用场景？
- [ ] **示例可用性**：示例命令是否完整可执行？
- [ ] **总览文件**：命令/API 数量 > 50 时，是否有总览文件按分类组织？
- [ ] **frontmatter**：每份 Cheat Sheet 是否包含 `article_id`、`tags`、`type`？

### 6.4 产出路径

```
explorer/cheatsheet/
├── all-commands-cheat-sheet.md          # 总览（大项目必须有）
├── {category1}-cheat-sheet.md           # 分类速查
├── {category2}-cheat-sheet.md
└── ...
```

同步更新：
- `CLAUDE.md` 笔记索引（添加 Cheat Sheet 条目）
- `.study-meta.json` 的 `topics[]`（location: "explorer"，子目录 "cheatsheet"）
